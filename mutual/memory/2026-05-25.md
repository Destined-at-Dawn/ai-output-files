# 2026-05-25 R16 整合治理

## 完成的工作

### R16 整合（Phase 2）
- **归档 572 个冗余文件**到 `%ARCHIVE%/2026-05-25-R16-consolidation/`
- 删除 5 个工作区的 `rules/` 目录（180 文件，与 .claude/rules/ 重复）
- 清理根目录模板文件（309 → 63，削减 79%）
- 清理通用 SOPs（129 → 44，削减 66%）
- 清理知识中枢模板（75 → 38，削减 49%）
- 更新 5 个 CLAUDE.md 的 R16 读取顺序
- 更新 R16 共享规则和工作区注册表

### 防回退治理（Phase 3）
- **修复 `rules/` 残留引用**：知识中枢 15+ 处 → 全部改为 `.claude/rules/`
- **创建防御规则**：`.claude/rules/no-root-rules-dir.md`（5 区部署）
- **哨兵文件**：5 个工作区根目录 `.NO-RULES-DIR-HERE.md`
- **脚手架修正**：scaffold-workspace SKILL.md 移除 `rules/` 创建步骤
- **_MIGRATED-TO-RULES.md**：从"引导创建 rules/"改为"禁止创建 rules/"（5 区同步）
- **5 个 CLAUDE.md 注入防回退铁律**（v5.2→v5.4）
- **3 个 `shared-rules/` 残留引用**修正为绝对路径
- **self-evolution/ SKILL.md** 补齐（创作/求职/竞赛从空壳升级）

### 关键决策
1. `rules/` 目录删除 → 规则在 `.claude/rules/`（Claude Code 自动加载）
2. 通用英文 SOP 删除 → 与 .claude/rules/ 内容重复
3. 工作区专属中文 SOP 保留 → 有实际工作流价值

### 4 个 R16 任务标记完成
- 共建「根目录优先约束架构」统一规则 R16
- 建立「主目录↔proj-ID」统一索引与 manifest 规范
- 4个工作区主目录补约束骨架与索引文件
- 治理重复索引与过期注册表条目

## 教训
- Phase 1 的 28 轮迭代创建了大量重复模板，每轮只是换了文件名复制到 5 个工作区
- 用户批评"迭代完全是敷衍"后，Phase 2 转为消除冗余的方法论
- **规则**：创建文件前必须确认该文件不会被现有机制覆盖（如 .claude/rules/ 自动加载）

### 新增 R17 会话启动协议
- 启动序列必须在 CLAUDE.md 顶部，Step 1-4 不可跳过
- 路径必须可达，禁止用"按需读取"跳过强制步骤
- 五区全部部署

### 新增教训（PK-ARCH-002/003）
- 迁移规则路径时需对**所有文件**做全量引用审计，否则旧引用引导 AI 重建已删目录
- `.claude/rules/` 中自动加载的文件如果内容错误，会成为"卧底"反向引导 AI
