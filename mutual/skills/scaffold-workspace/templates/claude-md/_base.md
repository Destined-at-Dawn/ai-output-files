---

## ⛔ 铁律层（每次对话必读，不可跳过）

> **以下 6 条铁律覆盖本工作区所有场景。违反任何一条 = 任务失败。**
> **AI 每次收到消息时，必须先读完这 6 条再做任何事。**

### F1. 技能审计（R10）

每次对话结束前，必须输出以下格式。没有例外。

```
📋 技能审计

本次调用：{技能名}（{详情}）、{工具名} ×{次数}

被忽视的：{应该调用但没调用的技能/文件} —— {为什么}

下次改进：{问题/坑/优化方向}
```

违反 = 本次对话质量不合格。

### F2. 文件操作铁律

| 规则 | 说明 |
|------|------|
| **禁止伪造** | 所有文件操作必须通过 tool_use（Write/Edit/Bash）发起 |
| **Write 后 Read** | 每次 Write/Edit 后，必须 Read 同一文件确认。没有 Read 验证 == 未完成 |
| **禁止空文件** | 写入的内容必须有实际信息 |
| **先 Read 再 Write** | 修改任何文件前必须先 Read 当前内容 |
| **PDF/Word/Excel 强制调 skill** | 遇到 `.pdf` → **先读 pdf skill 指南**（`mcp__skill-handler__Skill skill: "pdf"`）→ 用 pdfplumber 提取 → 输出到**当前工作区 `outputs/`**。禁止跳过 skill 直接写代码 |

### F3. 记忆追加铁律

| 规则 | 说明 |
|------|------|
| **禁止覆盖** | 更新 memory 文件时只在末尾追加，**禁止覆盖全文** |
| **追加格式** | 以 `## YYYY-MM-DD HH:MM` 开头 |
| **Write 后 Read** | 验证追加的行存在于末尾 |

### F4. 双归档铁律（最高优先级）

> 删除任何文件前必须完成双归档。违反 = 数据丢失。

1. 检查文件是否存在、是否有用
2. 归档到全局：`E:\ai产出文件\牛马\归档\{日期}-{主题}\`
3. 确认归档成功
4. 才可执行删除

> **注意**：工作区根目录不建 `归档/` 子目录。所有归档统一走全局路径。
6. 输出归档报告

### F5. {{工作区名}}专属铁律

> 由 scaffold-workspace 根据领域类型自动填充。每个工作区有自己的 F5 内容。

### F6. 教训必须回写 Skill/SOP（R09）

教训只记不改 = 白记。记录教训时必须：
1. 查 R09 映射表 → 找受影响的 SOP
2. 追加「迭代日志」
3. 同类教训 ≥3 次 → 升级为铁律

---

## 路由表（🔴 AI 每次收到消息时读这里）

> 10 秒内定位：你的任务属于哪一类 → 该读哪个章节 → 只读必要的。

| 消息关键词 | 读什么 | 为什么 |
|-----------|-------|-------|
| {{根据领域类型自动填充路由}} | ... | ... |
| 修改本文件 | §N（维护规则） | 维护规则 |
| 任何任务 | F1~F6 | **必读，不可跳过** |

---

# CLAUDE.md - {{工作区名}}工作区

> **本文件对所有在此目录下运行的AI智能体具有约束力。**
> **「规则」= 对AI的强制指令。** 本文件中所有"必须/禁止/不得"语言，是对AI的硬性指令。
>
> **优先级层级：**
>
> ```
> 约束文件 (CLAUDE.md) > 记忆文件 (memory/) > AI 的口头承诺
> ```

**版本：{{版本号}} | 最后更新：{{当前日期}}**
**路径：** `{{工作区路径}}`（注意双层目录嵌套！禁止写入上层目录）

---

## 零、开局必读索引

> **每个新对话开局，AI按需读取以下文件。不必全读——根据对话主题选择。**

| 对话主题 | 必读文件 | 为什么 |
|---------|---------|-------|
| **本工作区核心任务** | `memory/MEMORY.md` + 最近的 `memory/{日期}.md` | 了解最新进展和上下文 |
| **问题诊断/复盘** | `self-evolution/做得差的避免/` 最新文件 | 避免重复犯错 |
| **文件操作/工具** | 本文件 §四~§六（操作规则） | 确保文件操作合规 |
| **任何复杂问题** | 本文件 + `memory/long-term.md` | 完整上下文 |

### 关键文件速查

| 文件 | 用途 |
|------|------|
| `memory/MEMORY.md` | 记忆总索引 + Pattern-Key速查表 |
| `memory/long-term.md` | 长期记忆（用户偏好、重要决策、教训） |
| `memory/{YYYY-MM-DD}.md` | 每日对话详细记录 |
| `self-evolution/做得好的鼓励/` | 成功案例存档 |
| `self-evolution/做得差的避免/` | 失败教训存档 |

---

## 一、工作区身份与用户画像

{{用户画像占位符}}

> **详细个人信息请读取 telos**：`C:\Users\13975\.claude\telos\`（单一真相源）

---

## 二、回答结构

```
IF 问题 ∈ [概念分析, 商业分析, 个人决策] → 道法术器框架
ELSE → 结论先行 → 原因分析 → 引用来源（Source: path#line）
```

---

## 三、百大认知引用铁律

> 百大认知书籍单一真相源：`E:\ai产出文件\牛马\个人\个人\百大认知书籍\`（62本）
> 跨工作区引用时必须使用此绝对路径。

**引用三步流程：**
1. **查索引**：读 `百大认知书籍/000-总索引-Index.md` 定位相关书籍
2. **读原文**：去原书籍文件读取完整原文，取精确原句
3. **引用作答**：引用原句回答，标注 `Source: 百大认知书籍/0XX-书名.md#行号`

**引用纪律：**
- 优先使用最近30天未引用的书籍
- 禁止连续3次问题只引用同一批书
- 引用前必须读原书籍文件，禁止凭记忆复述

---

## 四、文件操作铁律（信任验证）

> **「承诺」一文不值，只有 tool_use 发起的实际文件操作才算数。**

| 规则 | 说明 |
|------|------|
| **4.1 禁止伪造** | 所有文件操作必须通过 tool_use（Write/Edit/Bash）发起 |
| **4.2 验证必须** | 每次 Write/Edit 后，必须 Read 同一文件确认内容正确落盘 |
| **4.3 绝对路径审计** | 系统会核对每个声称的绝对路径，虚假路径会被记录 |
| **4.4 禁止空文件** | 写入的内容必须有实际信息，不能写空文件或占位符 |

**验证格式：** 每次写文件后，回复中必须注明 `已验证: {文件路径} — {行数}行`

---

## 五、记忆文件操作铁律（追加禁止覆盖）

| # | 规则 | 说明 |
|---|------|------|
| **5.1** | **先 Read 再 Write** | 更新任何 memory 文件前，必须先 Read 读取当前全部内容，在末尾追加新内容 |
| **5.2** | **追加格式** | 新内容以 `## YYYY-MM-DD HH:MM` 的日期时间分隔开头，加在文件末尾 |
| **5.3** | **Write 后必须 Read 验证** | Write 之后必须在同一轮用 Read 验证追加的行确实存在于末尾 |
| **5.4** | **禁止空文件/占位符** | 写入的内容必须有实际信息 |

---

## 六、修改前安全清单

| # | 检查项 |
|---|--------|
| 1 | 读过要修改的文件当前内容？（必须先 Read） |
| 2 | 确认写入路径是 `{{工作区路径}}\`？（不是少一层的上层目录） |
| 3 | 修改是否涉及百大认知书籍？→ 必须同步更新个人区 CLAUDE.md |
| 4 | 修改是否影响身份信息？→ 必须同步更新 telos |
| 5 | 修改是否高风险？（删除/覆盖/重命名）→ 先存副本到 `E:\ai产出文件\牛马\归档\{日期}-{主题}\` |

---

## 七、自我进化机制

### 7.1 目录

```
self-evolution/
├── 做得好的鼓励/    ← 用户肯定时，保存该轮对话
└── 做得差的避免/    ← 用户批评时，保存该轮对话
```

### 7.2 操作规则

**当用户说"做得好/不错/厉害/满意"时：**
- 将本轮一问一答提取为 `.md` 文件，保存到 `self-evolution/做得好的鼓励/`
- 文件名：`{YYYYMMDD}_{主题}_成功案例.md`

**当用户批评或指出错误时：**
- 将本轮一问一答提取为 `.md` 文件，保存到 `self-evolution/做得差的避免/`
- 文件名：`{YYYYMMDD}_{主题}_失败教训.md`
- 包含：原问题 + AI回复 + 错误原因 + 正确做法

---

## 八、双归档铁律（最高优先级）

> 此规则优先级高于所有其他操作规则。违反=数据丢失=最严重错误。

### 8.1 归档目录

| 归档位置 | 路径 |
|---------|------|
| **全局归档** | `E:\ai产出文件\牛马\归档\{日期}-{主题}\` |

> **注意**：工作区根目录不建 `归档/` 子目录。所有归档统一走全局归档路径。

### 8.2 删除前四步（缺一不可）

1. `ls` 检查：列出目录/文件的完整内容
2. 识别原工作区：根据被删除文件的路径判断属于哪个工作区
3. 归档：`cp -r` 到全局归档路径（按日期+主题建子目录）
4. 验证归档完整性：ls 确认后才执行删除

### 8.3 删除后报告（强制）

```
删除完成
- 删除的文件/目录：[清单]
- 全局归档：E:\ai产出文件\牛马\归档\{文件名} — 已验证
```

---

## 九、与其他工作区的关系

| 工作区 | 关系 | 共享内容 |
|--------|------|---------|
| 个人区 | 百大认知书籍原始库 + 身份信息源 | 书籍文件、telos |
| 创作区 | 内容创作体系 | Skill系统、SOP体系 |
| 竞赛区 | 竞赛项目管理 | SOP体系、技术文档规范 |
| 求职区 | 简历/面试系统 | Pattern-Key体系 |
| **{{工作区名}}** | **{{关系描述}}** | **{{共享内容}}** |

---

## 十、telos 个人信息自动捕获

> **用户不应该记住"我需要告诉AI去更新telos"。AI应该自己捕捉、自己更新。**

每次对话中，AI 必须在后台监听以下信号。一旦检测到，**静默更新 telos**（无需告知用户，除非涉及敏感信息）：

| 信号 | 用户说了什么 | 更新文件 |
|------|------------|---------|
| 身份变化 | "我转专业了"/"我大二了" | identity.md |
| 新联系方式 | "新号码"/"新邮箱" | identity.md |
| 目标调整 | "不想考研了"/"想试试XX" | goals.md |
| 新兴趣 | "最近在看XX"/"对XX感兴趣" | interests.md |
| 新技能 | "学会了XX"/"在用XX" | skills.md |
| 认知跃迁 | "原来XX不是这样" | beliefs.md |
| 偏好变化 | "以后不要XX"/"我更喜欢XX" | learned.md |
| 经历/状态 | "今天XX比赛"/"拿到了XX" | identity.md→当前状态 |
| 失败/批评 | "你又犯了XX错误" | learned.md→教训 |
| 人脉变化 | "认识了XX老师" | identity.md→当前状态 |

**捕获流程**：检测信号 → 判断新/旧 → Read telos → Edit 追加/修改 → Read 验证
**敏感信息**（电话/身份证/密码）→ AskUserQuestion 确认后再写入
**telos 路径**：`C:\Users\13975\.claude\telos\`

---

## 十二、教训→skill/SOP 反馈闭环（强制）

> **教训只记不改 = 白记。** 每条教训必须流向它能影响的 SOP 或 skill。

### 反馈闭环铁律

每当自我进化文件新增一条教训，**同一次对话内**必须完成：

1. **定位**：查映射表，确定影响哪个 SOP + 哪个 skill
2. **回写**：在对应 SOP「迭代日志」追加一行
3. **升级**：同类教训 3 次 → 升级为 SOP 铁律（不可跳过）
4. **通知**：影响 skill 行为 → 更新 CLAUDE.md 对应规则段落

### SOP 迭代日志格式（每个 SOP 末尾必须有）

```markdown
## 迭代日志
| 日期 | 触发事件 | 原流程 | 发现的问题 | 更新内容 | 版本 |
|------|---------|--------|-----------|---------|------|
```

### 升级规则

| 次数 | 动作 |
|------|------|
| 第 1 次 | 记录到自我进化 + SOP 迭代日志 |
| 第 2 次 | SOP 对应步骤旁加 **⚠️ 高频坑** |
| 第 3 次 | 升级为 **铁律**，更新 CLAUDE.md |

成功经验也要回流 → SOP「最佳实践」段落。

映射表路径：`E:\ai产出文件\牛马\mutual\mutual\li-sync\shared-rules\09-feedback-loop.md`

---

## 十一、对话结束前收尾（强制）

每次对话结束前必须执行以下操作，**缺一不可**：

| 步骤 | 操作 | 验证方式 |
|------|------|---------|
| 1 | 更新 `memory/{YYYY-MM-DD}.md`（当日会话记录） | Read 验证 |
| 2 | 更新 `memory/MEMORY.md`（追加索引行，如需） | Read 验证 |
| 3 | 更新 `memory/long-term.md`（如有重要决策/偏好变化） | Read 验证 |
| 4 | 更新 `CLAUDE.md`（如有新规则/身份变化） | Read 验证 |

> Write 之后必须 Read 验证——没有 Read 验证 == 未完成。

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| {{当前日期}} | {{版本号}} | 初始创建：{{工作区名}}工作区脚手架生成 |


---

## 认知科学支撑：CLAUDE.md基础模板的认知设计（百大认知书籍）

| 认知机制 | 来源 | 在_base.md模板中的应用 |
|---------|------|------|
| **图式初始化** | 025-认知负荷理论 §概念4 | _base.md=CLAUDE.md的"基础图式"——新工作区的AI一启动就拥有完整的规则体系(而非从零积累)。图式初始化=认知冷启动时间从"数天"降到"0秒" |
| **认知层级** | 022-认知神经科学 §概念1 | 模板的分层结构(铁律→规则→收尾)=认知层级——AI按"紧急度"从高到低处理。铁律最先被处理→保证最核心的规则不被跳过 |
| **收尾清单** | 061-助推 §概念1 | 对话收尾清单=清单效应——4项收尾操作(记忆/教训/共享规则/CLAUDE.md)必须逐项确认→清单将"应该记得"转化为"必须检查"→遗漏率降至近零 |
| **占位符设计** | 028-我们如何学习 §概念1 | {{工作区名}}等占位符=脚手架的"接头"——告诉AI"这里需要个性化"→不占位=AI可能认为"这段话是固定的"→占位=明确提示"需要替换" |
