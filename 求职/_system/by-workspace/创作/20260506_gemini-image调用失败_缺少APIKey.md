- **失败原因**: 调用 `gemini-image` 技能时，其文档指向的 `config/secrets.md` 文件不存在，导致无法获取 API Key，生成流程中断。
- **错误假设**: 以为 Skill 的文档是永远正确的，以为所有依赖文件都已预先配置好。
- **正确做法**: 调用需要外部依赖（如API Key）的 Skill 前，必须先用 `list_project_files` 验证依赖文件是否存在。如果不存在，应立即向用户报告，并询问如何配置，而不是假设其存在并继续执行。
