# 电路PDF证据逻辑纠偏 — 下游 patch 建议

> Hermes 自动生成 | 2026-06-18
> 来源交接：`2026-06-18-Codex-电路PDF证据逻辑纠偏.md`

## 结论

用户已明确纠偏电路分析复习 PDF 的证据展示逻辑。Codex 已更新 `06_复习PDF制作SOP.md` 和项目 memory。以下 2 个下游文件需要 Codex 检查并按需 patch。

---

## 建议 1：检查 `日常学习/projects/20260603-电路分析/CLAUDE.md`

**原因**：SOP 已增加"成品验收门槛"（无原资料截图/视觉融合证据的 PDF 只能标为结构草稿），但 CLAUDE.md 可能仍保留旧的 Source 行号为主描述。

**检查点**：
- 搜索 `Source` 相关规则，若存在"Source 行号作为主要展示"的描述，需改为"原资料截图/AI视觉融合为主，Source 行号为辅助追溯"
- 搜索"禁止事项"区域，确认是否需要新增"禁止只给 Source 行号而不提供原资料截图证据"

**建议 patch 方向**（需 Codex 确认具体内容后执行）：
```
- old: 描述 Source 行号为主要展示方式的段落
+ new: 原资料对应区域截图/AI视觉融合展示为主要证据，Source 行号仅保留为辅助追溯（页脚/角落/索引）
```

---

## 建议 2：检查 `日常学习/projects/20260603-电路分析/AGENT.md`

**原因**：AGENT.md 定义了项目执行手册的输出链路和禁止事项。SOP 的证据逻辑变更可能需要同步到 AGENT.md 的以下位置：
- "禁止事项"区域：是否需要新增"禁止把 Source 表格/路径列表作为高要求 PDF 的页面主体"
- "输出链路"区域：image2 → LaTeX 链路是否需要强调"每页必须嵌入原资料截图证据"

**检查点**：
- 搜索 `Source` 或 `截图` 或 `证据` 相关描述
- 对比 SOP 的"成品验收门槛"段落，确认 AGENT.md 是否有冲突或遗漏

**建议 patch 方向**（需 Codex 确认具体内容后执行）：
```
- old: 禁止事项中关于 Source 的描述（若有）
+ new: 禁止只给 Source 行号而不提供原资料截图证据；禁止把 Source 表格/路径列表/索引页包装成高要求 PDF 成品
```

---

## 不需要修改的文件

| 文件 | 原因 |
|------|------|
| `06_复习PDF制作SOP.md` | Codex 已更新（成品验收门槛已加） |
| `memory/2026-06-18.md` | Codex 已记录（用户纠偏条目已写入） |
| `skill-routing-table.json` | r403 路由逻辑不变，只触发 SOP |
| `日常学习/CLAUDE.md`（根目录） | 无直接引用证据展示规则 |

## 建议执行方

**Codex** — 下次在电路分析项目工作时，读取 AGENT.md 和 CLAUDE.md，按上述检查点执行 diff，有变更则 patch。

---

*Hermes 角色限制：只读下游文件，不直接修改。以上建议由 Codex 判断后执行。*
