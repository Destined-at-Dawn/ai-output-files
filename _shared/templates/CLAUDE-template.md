# {工作区名称} - AI 工作区配置

## 变量说明
- ${WORKSPACE_ROOT} = 当前工作区根目录
- ${AI_ROOT} = F:i产出文件
- ${KNOWLEDGE_HUB} = F:i产出文件\知识中枢
- ${SHARED_RULES} = F:i产出文件\_sharedules
- ${NEWMAX_HOME} = ~/.newmax/

## 启动序列
1. Read ${WORKSPACE_ROOT}/runtime-snapshot.md
2. Read ${WORKSPACE_ROOT}/memory/long-term.md
3. Read ${WORKSPACE_ROOT}/memory/{today}.md
4. Read ${KNOWLEDGE_HUB}/00-注册表/工作区注册表.md

## 工作区规则
详见 .claude/rules/ 目录
