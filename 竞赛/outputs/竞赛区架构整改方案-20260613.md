# 竞赛区架构整改方案（2026-06-13）

> **状态**：待审阅，**未执行任何文件移动 / 未修改任何 skill / 未改任何规则**
> **触发**：`/goal` 核查"Skill 分离重构"目标是否真正闭环
> **结论**：目标约 85% 完成，上一轮产出真实且诚实，但有两个真实缺口未闭环

---

## 一、核查结论

### 1.1 已验证为真（磁盘证据）

| 子任务 | 证据 |
|--------|------|
| competition-yolo → v2.0 + 备份 | `~/.newmax/skills/competition-yolo/SKILL.md` 21815B + `.bak-20260613` |
| li-competition → v2.0 + 备份 | `~/.newmax/skills/li-competition/SKILL.md` v2.0.0 + `.bak-20260613` |
| competition-local.md 新建 | 主文件夹 + proj-xxx 各一份（字节相同 6121B） |
| lifecycle-log.md（"补录"属实） | `~/.newmax/skills/competition-yolo/lifecycle-log.md` |
| F13 / F14 / competition-workspace-architecture.md / skill-auto-activation 失败模式4 | 全部已落盘并已核对内容 |

> 说明：核查初期一度误判 lifecycle-log 不存在（搜索范围错放在 `竞赛/竞赛` 而非 `~/.newmax`）。在此更正——上一轮没有伪造。

### 1.2 两个真实缺口（未闭环的根因）

- **根因 A — Skill 拆分落在废弃位置，且拆分不彻底。**
- **根因 B — 五个并存"主文件夹" + 两条规则互相冲突。**

---

## 二、Part A：Skill 同步纠偏

### 2.1 执行"同步 v2.0 到 ~/.claude"时暴露的问题

当前 Claude Code 实际加载的 `li-competition` 在 `~/.claude/skills/`，仍是 v1.0；拆分全程只在 `~/.newmax/skills/` 进行，没生效到现在用的系统。但**直接覆盖会是退步**：

| 维度 | `~/.claude` v1.0（现役） | `~/.newmax` v2.0（拆分结果） |
|------|------------------------|---------------------------|
| 定位 | 真·通用硬件竞赛方法论 | 自称"通用"，实为 YOLO/电力专属 |
| 内容 | 逆向拆解、M1–M5 里程碑、风险矩阵、FPGA/RoboMaster/Vivado 三案例、理论锚点、反模式 | 实操6单元（ONNX量化）、理论4模块（电力巡检）、论文速查（TRS-YOLO…）、改进清单（NWD/Mosaic） |
| 适用 | FPGA / 电赛 / 数学建模 / 任意竞赛 | 仅本次 YOLO 电力竞赛 |

**判断**：原拆分没拆干净——项目专属内容没真正下沉到 `competition-local.md`，反而留在了"通用" skill 里。`~/.claude` 的 v1.0 才是更好的通用技能。**按字面覆盖 = 用"假通用"换掉"真通用"，并丢失 FPGA/RoboMaster 方法论。**

### 2.2 纠正原则

> 不是"v2.0 覆盖 v1.0"，而是"**保留 v1.0 通用骨架 + 注入 competition-local.md 指针，YOLO 专属内容下沉**"。

### 2.3 执行步骤（待确认后做，全程遵守 li-skillfusion：先备份、Write 后 Read、写 lifecycle-log）

1. 备份 `~/.claude/skills/li-competition/SKILL.md` → `.bak-20260613`
2. 用 **Edit 增量注入**"项目级配置（必读）→ 读 `{项目目录}/skills/competition-local.md`"指针段（保留全部 v1.0 通用内容，不覆盖）
3. 把 v2.0 残留的 YOLO/电力专属内容（6单元 / 4模块 / 论文速查 / 改进清单）**下沉到 `competition-local.md`**（它本就该在那）
4. `competition-yolo`：当前 `~/.claude` 不存在。**建议暂不创建**——它内容更杂、与 li-competition 职责重叠，应先在 Part B 决定主文件夹后再做干净拆分
5. Read 验证 + 追加 `lifecycle-log.md` 一条"同步纠偏"记录
6. 同步路由表（如该工作区有 skill-routing-table.json）

---

## 三、Part B：五文件夹合并方案

### 3.1 现状清点

| # | 文件夹 | 文件数* | 角色 | 含管理文件？ | F13 合规 |
|---|--------|--------|------|------------|---------|
| 1 | `Yolo算法比赛/`（工作区级） | ~12 | F13 指定主文件夹，骨架 | skills/competition-local.md | ✅ 名义合规 |
| 2 | `projects/Yolo算法比赛/` | 2845 | 重型工作目录（疑含数据集/.venv） | 待细查 | ⚠️ 同名歧义 |
| 3 | `projects/proj-1781073838312-332mv5/` | 72 | **实际产出全在这**（outputs/memory/报告/zip） | outputs/ memory/ skills/ self-evolution/ | ❌ proj-xxx 禁放管理文件 |
| 4 | `projects/20260609-YOLO绝缘子检测竞赛/` | 1 | 废弃骨架 | memory/ outputs/（空） | — 可删 |
| 5 | `projects/竞赛全流程/` | 112 | 竞赛**管理中枢**（CLAUDE.md/SOPs/路由表/自我进化） | 全套 | 独立用途，建议保留 |

\* 已排除 `.venv`/`.git`。

### 3.2 规则冲突（必须先解决）

| 规则 | 出处 | 说"主/产出在哪" |
|------|------|----------------|
| F13 + competition-workspace-architecture.md | 2026-06-13（新） | 主文件夹 = `Yolo算法比赛/` |
| 全局 CLAUDE.md「竞赛项目记忆路径覆盖」 | 全局（高优先级） | memory/outputs 只写 `竞赛全流程/` |

→ **两条规则对"YOLO 项目产出该放哪"给出不同答案。必须二选一，并同步改另一处，否则下次还会乱建。**

### 3.3 推荐目标态（建议，非定论）

- **唯一 YOLO 主文件夹** = 工作区级 `Yolo算法比赛/`（采纳更新、更具体的 F13）。
- `竞赛全流程/` 维持为**跨项目管理中枢**（不是 YOLO 专属，保留）。
- `proj-1781073838312-332mv5/` 的 72 个产出 → 分阶段迁入主文件夹后，原 proj 仅留 `project.md`/`task.json` 等任务跟踪文件。
- `projects/Yolo算法比赛/`（2845 文件）→ 先细查内容：数据集/.venv 应移到 D 盘（F13 允许代码/数据分离），有用产出并入主文件夹，消除"同名两目录"。
- `20260609-YOLO绝缘子检测竞赛/`（1 文件）→ 双归档后清理。
- **全局规则同步改为指向 `Yolo算法比赛/`**，消除 3.2 冲突。

### 3.4 迁移方法（分阶段，归档优先，逐步验证）

> 铁律：每步先 `python scripts/auto_archive.py <文件>` 双归档，再迁移；删除前 AskUserQuestion 确认；中文路径用 Python；每阶段 Glob/Read 验证落盘。

```
阶段0：定主文件夹 + 统一规则（人工拍板，本方案 §四）
阶段1：proj-xxx/outputs → 主文件夹/outputs（72文件，归档→复制→验证→留原档）
阶段2：proj-xxx/memory → 主文件夹/memory（合并，去重）
阶段3：proj-xxx/skills/competition-local.md 去重（只留主文件夹一份）
阶段4：projects/Yolo算法比赛 数据集/.venv → D盘；有用产出并入主文件夹
阶段5：20260609 双归档后清理；proj-xxx 仅留任务跟踪文件
阶段6：competition-local.md 内的相对路径引用全部校正
阶段7：全局 CLAUDE.md + F13 规则一致性复核
```

### 3.5 风险与回滚

| 风险 | 缓解 |
|------|------|
| 迁移中断导致产出丢失 | 每文件先双归档（全局+工作区），原文件保留到验证通过 |
| competition-local.md 引用断链 | 阶段6 专门校正相对路径 |
| 2845 文件目录误迁数据集 | 先细查分类，数据/.venv 走 D 盘不进迁移 |
| 改规则引发其他工作区不一致 | 用 li-sync 跨工作区一致性检查 |

---

## 四、需要你拍板的 4 个点

1. **主文件夹定哪个？**（建议：工作区级 `Yolo算法比赛/`）
2. **F13 vs 全局规则，以谁为准？**（建议：以 F13 为准，改全局规则指向 `Yolo算法比赛/`）
3. **Skill 纠偏是否按 Part A 执行？**（保留 v1.0 通用 + 注入指针 + 专属内容下沉）
4. **`competition-yolo` 去留？**（建议：暂不在 ~/.claude 创建，主文件夹定了再做干净拆分）

---

## 五、边界声明

- ✅ 本方案**只读 + 写本文档**，未移动/删除任何文件，未改任何 skill/规则。
- ⚠️ `projects/Yolo算法比赛/` 的 2845 文件尚未逐一分类，详细去向表需阶段4 专门扫描后产出。
- ❓ "全局规则 vs F13 谁为准"属于你的架构决策，本方案只给建议，不替你决定。

> 产出位置：`竞赛/竞赛/outputs/竞赛区架构整改方案-20260613.md`
> 版本：v1.0
