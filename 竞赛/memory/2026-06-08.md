# 2026-06-08 记忆

## 任务概要
用户要求学习 FPGABuilder (github.com/YiHok/FPGABuilder) 并改进 skill/SOP → 发现技能自动激活系统性失败 → 全量修复路由基础设施。

## 关键发现

### FPGABuilder 学习成果
- 10个可复用设计模式（配置驱动/插件架构/Hook系统/TCL模板化等）
- 已注入 hardware-design SKILL.md v5.0、SOP-06 v1.9
- 成功案例归档：`自我进化/做得好的鼓励/20260525_FPGABuilder…_成功案例.md`

### 🔴 技能自动激活系统性失败（本日最重要）
- **事故**：上一轮对话中 AI 在 11/11 个环节零次调用 skill，全部手动完成
- **根因 1**：双路由文件混乱（skill-routing-table.json vs skill-rules.json 格式不同、内容冲突）
- **根因 2**：路由表缺少 li-* 系列 skill 触发词（0条→现已注册）
- **根因 3**：`.claude/rules/skill-auto-activation.md` 文件不存在（CLAUDE.md 引用了空路径）
- **根因 4**：AI 把 skill 当"可选参考"而非"必须执行的工作流引擎"

### 修复清单（4项全部完成）
1. `.claude/skill-rules.json` → 已加 _deprecated 标记，指向 skill-routing-table.json
2. `CLAUDE.md` §Skill路由段 → 统一引用，清除三层路由说法
3. `MEMORY.md` → 更新过期引用
4. `.claude/rules/skill-auto-activation.md` → 创建 v1.0（硬协议：扫描→匹配→调用→审计）

### 新对话启动检查清单
详见 `outputs/handoff-20260608.md`。核心：新对话的 AI 必须先读取 skill-auto-activation.md 硬协议，再匹配路由表。

### skill-routing-table.json v2.1 深度审计（另一个对话的诊断 + 本轮修复）

**另一个对话的尖锐指控**：11/11 个应用环节零 skill 调用。路由表里 0 条 li-* skill、0 条 resume-modification 路由。skill 被当成"可选参考文件"而非"可执行工作流工具"。

**本轮深度审计结果（v1→v2.1）**：

| 问题 | v1 状态 | v2.1 修复 |
|------|---------|----------|
| 幽灵 skill（未安装但有路由） | 6 个（xhs-content-strategist/weather-forecast/context-compression/auto-evolution/feishu-doc-writer） | 0 个 |
| 重复路由（同一 skill 多条） | 21 个 skill 有 2-6 条路由 | 合并为 1 条/skill |
| 总路由数 | 111 条 | 72 条 |
| P1 优先级 | 31 条（无区分度） | 11 条（真正核心） |
| 关键 skill 缺失 | systemverilog/longmemory/find-skills 投流帖改写 | 全部补录 |
| SOP 路由缺失 | SOP-08/10/11 触发词为 0 | 全部补录 |
| ID 编号 | r001~r115，有跳号 | r001~r072，连续 |

**CLAUDE.md v3.8 强化**：
- 技能自动激活段新增"强制预检流程"：P1 匹配必调用 / P2+ 声明激活 / 无匹配声明
- 版本号 v3.7→v3.8

## 决策记录
- 单一真相源：skill-routing-table.json v2.1（72条路由，Step 4.5 自动加载）
- skill-rules.json：保留但加废弃标记，禁止 AI 依赖
- 路由层级：简化为两层（skill级 + SOP级），不再提"三层并行"
- skill 名修正：claude-skill-verilog → systemverilog（与实际目录名一致）
- 路由表维护铁律：新增/删除 skill 时必须同步更新路由表，每月全量审计

## 待办
- [ ] 下次对话验证 skill 自动激活是否实际生效（≥80% 调用率）
- [ ] 如仍失败 → 升级为独立 Skill（同类踩坑 ≥3 次）
- [ ] 其他4个工作区的路由表也需要同步审计（创作/求职/个人/知识中枢）
