# 2026-05-29 ADC closeout

- 2026-05-30 EEPROM failure after LED pin fix: boss saw `D1=1, D0=0` and suspected the AT21CS01 device address bits, not the FPGA part number. Updated standalone `D:/AMD/eeprom_rw` so `at21cs01_master` no longer uses a fixed `.DEVICE_ADDR(3'b000)`. It now scans address `0..7`, retries the write/read/compare sequence after each NACK or mismatch, latches `found_addr` on success, and fails only after all 8 addresses fail. Archive before change: `D:/AMD_archives/eeprom_rw_before_addr_scan_20260530_153146.zip`. New package: `D:/AMD/EEPROM单总线读写_自动扫描地址_D0成功D1失败_20260530.zip`.
- 2026-05-30 process upgrade: user requested every RTL correction to become a reusable database for future first-pass RTL work. Created `memory/rtl_code_lessons.md`, `self-evolution/rtl-one-pass-sop.md`, and a new `rtl-fpga-lessons` skill in both `C:/Users/13975/.agents/skills/rtl-fpga-lessons` and workspace `skills/rtl-fpga-lessons`. Future RTL/Vivado tasks should read those before editing and append new lessons after verification.
- 2026-05-30 Claude sync: user asked to sync the RTL skill workflow into `.claude`. Added `.claude/rules/rtl-fpga-lessons.md` as the Claude-side rule entry. Did not edit `.claude/skill-rules.json` because its existing content shows encoding/validity risk; keeping the workflow as a standalone rule avoids corrupting the router.
- 2026-05-30 ADC/DAC target clarification: user wants theoretical 0-75 kHz arbitrary waveform sampling and reconstruction with stability. Engineering conclusion recorded in RTL lessons: this cannot be guaranteed on the current one-shared-I2C ADC-read plus DAC-write chain. Nyquist 150 ksps is only a lower bound; stable waveform shape needs more samples per cycle, deterministic sample clocks, adequate DAC settling, analog filters, and a faster/separate data path.
- 2026-05-30 ADC/DAC stable delivery: after boss said the high-speed ADC/DAC version was worse than the fixed 10 Hz version, archived the rejected high-speed project at `D:/AMD_archives/dac_adc1_loopback_highspeed_rejected_by_boss_20260530_1610.zip`. Restored the pre-high-speed ADC1-to-DAC1 replay baseline, kept I2C at 400 kHz, changed `ADC_SAMPLE_DIV` to 25000 for a 2 kHz request rate (~200 samples per 10 Hz cycle), rebuilt through bitstream, and packaged `D:/AMD/ADC1采样_DAC1稳定回放_10Hz示波器对比工程_20260530.zip`.

- All five split projects under `D:/AMD/ADC/experiments` were rebuilt with Vivado 2020.1 through implementation and bitstream generation on 2026-05-29.
- Important fix: the split projects originally had only `MARK_DEBUG` nets and no ILA debug core. That meant ADC/DAC internal debug signals could be optimized away and SDA input showed the `BUFC-1` no-load warning. Added shared `setup_ila_current_project.tcl` and sourced it from every `run_bitstream.tcl` before implementation.
- Final deliverables now include `.bit` and `.ltx` for all five projects. Timing reports say all user timing constraints are met.
- Remaining DRC entries are warnings from debug hub `RTSTAT-10`; ADC projects also have one `IOSR-1` IOB packing optimization warning. These are not functional failures.
- Physical board validation is still a hardware step: scope DAC output, use ILA for ADC samples, and use ILA/board signals for EEPROM readback comparison.
- Created teaching DOCX files matching the existing experiment document structure under `D:/AMD/project_output/实验文档`: Experiment 6 DAC triangle, Experiment 7 ADC ILA capture, and Experiment 8 1-WIRE EEPROM read/write. Packaged them into three individual project ZIPs and one combined ZIP with docs.
- Boss feedback: the first document version was praised, but future work should target company customer-delivery quality, not school-report quality. New SOP saved at `D:/AMD/project_output/实验文档/实验教学文档重写SOP_20260529.md`.
- Customer-delivery doc strategy: study the official ETL4-7A35T PDF manual first, replicate its A4 single-column layout, centered company header, page-number footer, chapter-based numbering, real operation screenshots, figure captions, red warnings, file hyperlinks, and concise engineering explanations. Generated customer-delivery DOCX drafts for chapters 7/8/9 plus one combined ADC/DAC/EEPROM extension manual.
- Scope/probe mismatch lesson: if the old unified `D:/AMD/ADC` bitstream shows a waveform but a split project does not, first check project-to-probe alignment. The old top writes both DAC addresses (`0x09` and `0x0a`) in one design, so either DAC output can show activity. The split DAC projects write only one DAC each: `01_dac1_triangle_1khz` writes address `0x09`, while `02_dac2_sine_1khz` writes address `0x0a`. `03_adc1_capture` and `05_eeprom_rw` are not analog waveform output projects; they are meant to be verified through ILA/debug signals. For future demos, document exactly which `.bit/.ltx` is programmed and which physical output/probe point is measured.
- Packaging correction: boss's wording means 3 category zip packages, not only 3 individual projects. Correct category packages generated on 2026-05-29: DAC package contains `01_dac1_triangle_1khz` and `02_dac2_sine_1khz`; ADC package contains `03_adc1_capture` and `04_adc2_capture`; EEPROM package contains `05_eeprom_rw`. Each package includes `setup_ila_current_project.tcl`, the category/customer DOCX, and the manifest so `run_bitstream.tcl` can still find the shared ILA setup script after extraction.
- DACcode=0 fix on 2026-05-29: `dbg_dac_code` can look stuck at 0 because original ILA depth 4096 samples covers only 81.92 us at 50 MHz, while DAC code updates every 6250 clocks / 125 us. Increased shared ILA `C_DATA_DEPTH` to 16384, added DAC debug probes `dbg_rst_n` and `dbg_i2c_busy`, cleaned DAC XDC debug constraints before regeneration, rebuilt DAC1/DAC2 with Vivado 2020.1, and repackaged the DAC category zip plus DAC-containing backup zips.
- DAC1 10Hz rework on 2026-05-29: user requested archiving old `D:/AMD/dac1_triangle_1khz` content before rebuilding. Old content is archived at `D:/AMD_archives/dac1_triangle_1khz_original_20260529_143853` and zip. Boss board feedback said DAC1/DAC2 outputs were swapped, so new DAC1 project uses `DAC_ADDR=7'h0a`. Boss also said 1kHz was too fast and only 8 points appeared per cycle; new design uses `UPDATE_DIV=6250`, `POINT_COUNT=800`, giving `50MHz/6250/800=10Hz`. First smooth version inferred DSP multiply/divide and had DSP pipeline DRC warnings; final version uses an accumulator/Bresenham triangle generator and rebuilds with Vivado 2020.1 to bit/ltx. Source `.v/.tcl/.xdc` files stay ASCII to avoid comment mojibake.
- DAC1 sine/ADC loopback/EEPROM LED rework on 2026-05-29: user clarified `D:/AMD/dac1_triangle_1khz` is the boss-approved 10Hz triangle baseline and had already been sent as a zip. Before changing it for sine output, archived the baseline at `D:/AMD_archives/dac1_triangle_1khz_before_sine_adc_20260529_173014.zip`. Current `D:/AMD/dac1_triangle_1khz` now outputs DAC1 10Hz sine-like waveform using `dac_i2c_sine_wave_ctrl`, `UPDATE_DIV=6250`, `POINT_COUNT=800`, and `DAC_ADDR=7'h0a`; old triangle controller and old triangle handoff were removed from the current deliverable to avoid confusing the boss.
- New combined DAC+ADC project `D:/AMD/dac_adc1_loopback_10hz` makes DAC1 output a 10Hz triangle and reads ADC1 on the same I2C bus for ILA capture. Important design choice: use one shared `i2c_byte_master` scheduler, give DAC updates priority, and expose `missed_dac_update_sticky` so hardware can prove the 10Hz/800-point schedule is not being starved by ADC reads. Parameters are `DAC_UPDATE_DIV=6250`, `DAC_POINT_COUNT=800`, `ADC_SAMPLE_DIV=50000`, `DAC_ADDR=7'h0a`, `ADC_ADDR=7'h55`.
- EEPROM independent project `D:/AMD/eeprom_rw` now has LED pass/fail outputs: reset leaves `D0=0` and `D1=0`; after read/write/compare, `D0 = done & pass`, `D1 = done & fail`. LED pins follow the ETL4 LED example: `D0` on AB20 and `D1` on AA21. Built with Vivado 2020.1 through bitstream. All three final project source files `.v/.tcl/.xdc` passed ASCII check; timing reports say user constraints are met; remaining bitgen warning is Vivado debug hub `RTSTAT-10`.
- 2026-05-30 boss hardware feedback: DAC1 sine output is stable and clean; ADC1 has waveform but ILA cannot conveniently show a full 10Hz cycle. New validation strategy is ADC1 first samples an external 10Hz analog waveform, then DAC1 replays the captured samples, so a two-channel oscilloscope can compare original ADC input against DAC reconstructed output. Updated `D:/AMD/dac_adc1_loopback_10hz` in place: `dac_adc1_loopback_ctrl` now reads ADC1 at 1kHz and writes the latest UQ8.0 sample to DAC1. Also corrected EEPROM LED pins per boss: `D0=AB22`, `D1=AB21`. Archives before this rework: `D:/AMD_archives/eeprom_rw_before_led_pin_fix_20260530_1115.zip` and `D:/AMD_archives/dac_adc1_loopback_before_adc_replay_20260530_1115.zip`. New zips: `D:/AMD/ADC1采样_DAC1回放_双通道示波器对比工程_20260530.zip` and `D:/AMD/EEPROM单总线读写_D0_AB22_D1_AB21指示工程_20260530.zip`.
- 2026-05-30 later boss feedback: replay waveform top/bottom clipped and boss requested no fixed signal frequency, target highest practical sample rate. Important engineering conclusion: ADC081C021 conversion rate can be around 188 ksps, but ADC-read plus DAC-write on the same I2C bus cannot deliver 150 ksps end-to-end; I2C transaction bits are the bottleneck, not FPGA timing. Updated `D:/AMD/dac_adc1_loopback_10hz` again: removed fixed `ADC_SAMPLE_DIV`, set I2C target to 3.4 MHz with rounded 3.125 MHz SCL at 50 MHz clock, initializes ADC pointer once, then continuously reads ADC1 two bytes and writes the sample to DAC1. New zip: `D:/AMD/ADC1高速连续采样_DAC1实时回放工程_20260530.zip`. Hardware note: avoid full rail input like 3.3 Vpp with 1.65 V offset; 3.0 Vpp with 1.65 V offset is safer to avoid ADC/DAC rail clipping.
