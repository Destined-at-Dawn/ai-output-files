# 2026-05-28

## RTL history review
- Scope read: workspace memory, CLAUDE/AGENT rules, SOP-06 hardware verification SOP, RTL/XDC/TCL outputs, self-evolution success/failure notes, and readable manual sources/logs/reports under D:\AMD.
- Repeated AI failure modes: inventing bugs from diffs, guessing before reading Vivado logs, mishandling large ROM/IP flows, corrupting non-UTF8/GBK files with naive text tools, mismatched CMD constants across modules, non-open-drain I2C SDA, false global ACK detection outside case states, and reading memory without executing it.
- Source-of-truth rule: root outputs and many files at D:\AMD root are old/intermediate/backup versions. Before any RTL task, confirm true source directory, version, encoding, toolchain, XDC, and verification target.

## D:\AMD review
- D:\AMD\ctp\ctp.srcs\sources_1\imports\code is one current touchscreen source tree. Its touch_ctrl.v header records the 2026-05-22 I2C protocol fix: i2c_master only sends wr_data and does not auto-send addr_rw; correct flow is START -> WRITE(0x70) -> WRITE(reg) -> ReSTART -> WRITE(0x71) -> READ -> STOP.
- D:\AMD\ctp builds cleanly at RTL/implementation level: synthesis has 0 errors and 0 critical warnings, routed route errors are 0, and routed setup/hold have 0 failing endpoints. The visible failures in vivado.log are mainly hw_server/JTAG/ILA connection failures, not RTL synthesis failures.
- Touchscreen versions are split: D:\AMD\ctp outputs 16-bit raw touch_x/touch_y, while D:\AMD\touch_4bit, D:\AMD\screen_4bit, and D:\AMD\touchscreen are 4-bit 0..15 mapping/display variants. Confirm raw-coordinate vs 4-bit-mapped behavior before editing.
- I2C checklist: CMD_* encodings must match between i2c_master.v and touch_ctrl.v; top-level SDA must be open-drain, only pull low and never actively drive high; ACK checks should be per write-address/register/read-address states, not a global case-external check.
- D:\AMD\tft_logo and D:\AMD\project_output\tft_logo\code are the 16-bit RGB565 delivery version. D:\AMD root create_project.tcl plus root logo_rom.v/lcd_top.v/lcd_top.xdc are earlier 24-bit/old-port/old-pin versions and must not be mixed with the delivery tree.
- TFT Logo rule: for Vivado, do not infer BRAM with $readmemh when ROM depth is >16K. Use Block Memory Generator IP plus .coe. Handle IP file objects/OOC properties after generate_target all. Static wrapper module name must match the IP name.
- Older spi_RGB565/spi_XC7A35T logbooks record the same path: the project first used RGB888/24-bit ROM and later moved to RGB565/16-bit delivery. They also record TB issues: fork/join_any and disable fork are SystemVerilog-in-.v problems for XSIM; simulation top name must match the actual module; Vivado 2020.1 does not support launch_simulation -behavioral.
- The older simulation logbook says all 153600 pixels and 16 commands completed but 7 verification errors remained; later notes say the TB capture lag was fixed and an out-of-range DUT ROM address warning was functionally masked by the non-logo white mux. Do not treat the old TB warning as proof of current RTL failure without re-running the current source.
- D:\AMD\tft_logo routed timing passes, but DRC reports RAMB async control warnings because BRAM address/enable related registers use async reset. Future quality work should convert BRAM-driving address/enable registers to sync reset or no reset.
- Vendor/demo folders under CL35BC1017-40A_CTP and material are reference code for ST7796S/FT6336U protocol only, not FPGA RTL truth. Use them to confirm register addresses, I2C transaction shape, and LCD init command intent; do not port blindly or label differences as bugs without waveform/log evidence.
- Encoding rule: many D:\AMD Chinese comments/logs display as mojibake in default PowerShell. Do not conclude source is corrupted from console mojibake. Avoid scanning compressed/binary files as text.

## ADC/DAC/EEPROM demo delivery
- Delivered a new isolated project under D:\AMD\ADC instead of modifying old D:\AMD root/intermediate projects.
- Top-level ports are limited to clk, reset, i2c_scl, i2c_sda, and DQ because the user only provided constraints for those pins; no LED port was added to avoid unconstrained-port DRC issues.
- The I2C pins and EEPROM DQ are open-drain in RTL and have PULLUP enabled in XDC. This follows the previous I2C lesson: release for logic high, only actively drive low.
- Vivado 2020.1 was available through D:\Vivado\2020.1\bin\vivado.bat, but this execution environment lacked PROCESSOR_ARCHITECTURE. Setting PROCESSOR_ARCHITECTURE=AMD64 before calling vivado.bat fixes the silent loader exit.
- The ADC/DAC/EEPROM project at D:\AMD\ADC now builds through synthesis and write_bitstream with Vivado 2020.1. A debug ILA core is saved into the XDC and produces both adc_dac_eeprom_top.bit and adc_dac_eeprom_top.ltx.
